---
phase: 03-also-remove-the-portfolio-tab-with-all-relevant-code-and-assets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - assets/js/script.js
  - assets/css/style-minimalist.css
  - assets/images/neattcp.jpg
  - assets/images/eray.jpg
  - assets/images/plab.jpg
  - assets/images/tk4.jpg
  - assets/images/noteplan_kibela.jpg
  - assets/images/youtify.png
autonomous: true
requirements:
  - PTAB-01
  - PTAB-02
  - PTAB-03
  - PTAB-04
  - PTAB-05

must_haves:
  truths:
    - "The navbar shows only About and Resume tabs — no Portfolio tab exists"
    - "No portfolio content (projects, filters, modal) is visible on any page"
    - "The site loads with zero console errors — no JS references to removed DOM elements"
    - "Portfolio project images are deleted from the repository"
    - "No dead CSS remains for portfolio or theme switcher features"
  artifacts:
    - path: "index.html"
      provides: "Two-tab site (About + Resume) with no portfolio or modal markup"
      contains: "data-nav-link"
      not_contains: "data-page=\"portfolio\""
    - path: "assets/js/script.js"
      provides: "Sidebar toggle and page navigation only"
      not_contains: "portfolioModalFunc"
    - path: "assets/css/style-minimalist.css"
      provides: "Styles for About and Resume pages only"
      not_contains: ".filter-list"
  key_links:
    - from: "assets/js/script.js"
      to: "index.html [data-page] articles"
      via: "navigationLinks click handler matching innerHTML to dataset.page"
      pattern: "pages\\[i\\]\\.dataset\\.page"
---

<objective>
Remove the Portfolio tab, all its HTML/JS/CSS code, the modal overlay, dead contact form JS, leftover theme switcher CSS, and delete the 6 portfolio project images.

Purpose: The portfolio section adds complexity and assets that are no longer wanted. Removing it leaves a clean two-tab site (About + Resume) with zero dead code.
Output: A leaner index.html, script.js, and style-minimalist.css; 6 fewer image files in the repo.
</objective>

<execution_context>
@/Users/kaywallaschek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaywallaschek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@index.html
@assets/js/script.js
@assets/css/style-minimalist.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip portfolio HTML and JS from index.html and script.js</name>
  <files>index.html, assets/js/script.js</files>
  <action>
  In index.html, remove these three blocks:

  1. The Portfolio navbar item — the `<li class="navbar-item">` containing the button with text "Portfolio" (~line 170-172). Keep the About and Resume list items.

  2. The entire Portfolio article — `<article class="portfolio" data-page="portfolio">` through its closing `</article>` (~lines 506-698). This contains the filter list, filter select box, and all 7 project items.

  3. The modal container — `<div class="modal-container" data-modal-container>` through its closing `</div>` (~lines 700-725). This contains the overlay, portfolio-modal section, close button, and modal content divs.

  In script.js, remove everything EXCEPT the sidebar toggle and page navigation code. Specifically, keep:
  - Lines 1-15: element toggle function, sidebar variables, sidebar click handler
  - Lines 124-144: page navigation variables and nav link event listeners

  Remove ALL of the following:
  - Lines 19-39: portfolio variables (portfolioItem, modalContainer, modalCloseBtn, overlay), modal variables (modalImg, modalTitle, modalText), portfolioModalFunc function, and modal close/overlay click event listeners
  - Lines 43-99: custom select variables (select, selectItems, selectValue, filterBtn), select click handler, select item event loop, filter variables (filterItems), filterFunc function, and filter button event loop
  - Lines 103-120: contact form variables (form, formInputs, formBtn) and form input event loop — these reference `[data-form]`, `[data-form-input]`, `[data-form-btn]` elements that do NOT exist in the HTML, making this dead code

  After editing, script.js should contain approximately 25 lines: the 'use strict' directive, elementToggleFunc, sidebar toggle, and page navigation handler.
  </action>
  <verify>
  1. `grep -c "portfolio" index.html` returns 0
  2. `grep -c "modal" index.html` returns 0
  3. `grep -c "data-filter" index.html` returns 0
  4. `grep -c "filter" assets/js/script.js` returns 0
  5. `grep -c "modal" assets/js/script.js` returns 0
  6. `grep -c "form" assets/js/script.js` returns 0
  7. Count of `data-nav-link` in index.html equals 2 (About, Resume)
  8. Count of `data-page` in index.html equals 2 (about, resume)
  </verify>
  <done>index.html has only About and Resume tabs with no portfolio/modal/filter markup. script.js contains only sidebar toggle and page navigation — no dead portfolio, filter, modal, or form code.</done>
</task>

<task type="auto">
  <name>Task 2: Remove portfolio CSS, leftover theme switcher CSS, and delete portfolio images</name>
  <files>assets/css/style-minimalist.css, assets/images/neattcp.jpg, assets/images/eray.jpg, assets/images/plab.jpg, assets/images/tk4.jpg, assets/images/noteplan_kibela.jpg, assets/images/youtify.png</files>
  <action>
  In style-minimalist.css, remove these sections:

  1. The entire `#PORTFOLIO SECTION` block (~lines 540-631) — includes .filter-list, .filter-select-box, .filter-item, .project-list, .project-item, .project-img, .project-title, .project-category, .project-item-icon-box styles.

  2. Inside the `@media (max-width: 768px)` block (~line 637), remove the `.project-list { grid-template-columns: 1fr; }` rule (~lines 690-692). Keep all other responsive rules in that media query.

  3. The entire `#THEME SWITCHER - Minimalist Design` section (~lines 714-968) — this is leftover dead CSS from Phase 1 that was missed during the theme infrastructure strip. Includes .theme-switcher-nav, .theme-switcher-btn, .theme-icon, .theme-text, .dropdown-icon, .theme-dropdown, .theme-option, .theme-preview, .neo-tokyo-preview, .minimalist-preview, .original-preview, .checkmark, .theme-prompt, .theme-prompt-arrow, .theme-prompt-text, and related @keyframes and responsive overrides. Delete everything from the `#THEME SWITCHER` comment through end of file.

  Then delete these 6 portfolio project image files:
  - `assets/images/neattcp.jpg`
  - `assets/images/eray.jpg`
  - `assets/images/plab.jpg`
  - `assets/images/tk4.jpg`
  - `assets/images/noteplan_kibela.jpg`
  - `assets/images/youtify.png`

  After editing, the CSS file should end after the `@media (max-width: 480px)` responsive block. The images directory should contain only: my-avatar_3.png, icon-design.svg, icon-dev.svg, icon-photo.svg, logo.ico.
  </action>
  <verify>
  1. `grep -c "filter-list\|project-list\|project-item" assets/css/style-minimalist.css` returns 0
  2. `grep -c "theme-switcher\|theme-dropdown\|theme-option" assets/css/style-minimalist.css` returns 0
  3. `ls assets/images/ | grep -v DS_Store | wc -l` returns 5 (avatar, 3 icons, favicon)
  4. `ls assets/images/neattcp.jpg 2>&1` returns "No such file"
  5. CSS file ends cleanly after the 480px media query — no trailing theme switcher rules
  </verify>
  <done>Portfolio CSS section gone, theme switcher dead CSS gone, CSS file is clean. Only 5 non-portfolio images remain in assets/images/.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full site integrity:

1. **No portfolio references anywhere:**
   - `grep -ri "portfolio" index.html assets/js/ assets/css/` returns nothing
   - `grep -ri "filter-item\|filter-btn\|project-item" index.html assets/js/ assets/css/` returns nothing

2. **No modal/overlay references:**
   - `grep -ri "modal\|overlay" index.html assets/js/ assets/css/` returns nothing

3. **Navigation works (2 tabs only):**
   - index.html contains exactly 2 `data-nav-link` buttons and 2 `data-page` articles
   - script.js navigation handler correctly maps nav link text to page dataset

4. **No dead JS code:**
   - script.js contains no references to elements not present in index.html

5. **Clean assets directory:**
   - `ls assets/images/` shows exactly 5 files (my-avatar_3.png, icon-design.svg, icon-dev.svg, icon-photo.svg, logo.ico)

6. **No console errors:**
   - Open site in browser or Playwright, confirm zero JS errors in console
</verification>

<success_criteria>
- The site has exactly 2 navigation tabs: About and Resume
- Zero portfolio, modal, filter, or theme switcher code remains in HTML, JS, or CSS
- Six portfolio images deleted from repository
- Page navigation between About and Resume works correctly
- No JavaScript console errors on page load
</success_criteria>

<output>
After completion, create `.planning/phases/03-also-remove-the-portfolio-tab-with-all-relevant-code-and-assets/03-01-SUMMARY.md`
</output>
